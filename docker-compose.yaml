services:
    stundenplan:
        build: .
        image: stundenplan:dev
        volumes:
            - ./resources:/app/src/python/resources
        command: ${COMMAND:--help}
        ports:
            - "1111:80"